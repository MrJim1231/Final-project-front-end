⚡ **Важно:** В разработке проекта активно использовался искусственный интеллект (ChatGPT) — для генерации частей кода, оптимизации архитектуры, написания документации и ускорения рутинных процессов.  
При этом вся структура, логика, постановка задач и итоговая интеграция выполнены вручную.

# 📌 Task Management Dashboard (React + Redux Toolkit)

Task Management Dashboard — это современное приложение для управления задачами, разработанное с использованием **React**, **Redux Toolkit**, **TypeScript** и **Vite**.  
Проект создан как курсовая работа и демонстрирует полноценную архитектуру, продуманную логику и масштабируемую структуру.

---

## 🚀 Основные возможности

### ✔ Управление задачами

- Добавление задач
- Редактирование
- Удаление
- Изменение статуса (Not Started → In Progress → Completed)
- Поддержка приоритетов: **Extreme / Moderate / Low**

### ✔ Фильтрация

- По дате создания
- По типу страницы:
  - My Tasks
  - Vital Tasks
  - Completed Tasks

### ✔ Пагинация

- Отдельная пагинация для каждой категории:
  - My Tasks
  - Vital Tasks
  - Completed Tasks
- Сохранение состояния пагинации при переключении между страницами

### ✔ Поиск

- Поиск по `title` и `description`
- Поиск работает по **всем датам и категориям** одновременно

### ✔ Vital Tasks

- Отметка задачи как **важной** через поле `vital`
- Отдельная страница **Vital Tasks** для управления важными задачами

### ✔ Completed Tasks

- Автоматический перенос завершённых задач в категорию **Completed**
- Отдельная страница завершённых задач
- Отображение последних выполненных задач с корректной логикой выбора активной карточки

### ✔ Task Details

- Подробная информация о задаче (описание, дата, статус, приоритет)
- Корректная работа при пагинации и смене страницы
- Поддержка мобильного интерфейса
- Fallback-логика: если на выбранную дату задач нет — показываются задачи предыдущей доступной даты

## ✔ Управление категориями (Categories)

В проекте реализован модуль управления системными справочниками:

- **Task Categories** — задел на будущую функциональность
- **Task Status** — статусы задач
- **Task Priority** — приоритеты задач

Каждый справочник имеет отдельную страницу в разделе **Categories**, где доступны:

- ➕ добавление новых элементов
- ✏️ редактирование
- 🗑 удаление

### 📌 Страница Categories включает:

- Настройку **Task Categories**
- Настройку **Task Status**
- Настройку **Task Priority**

---

### 🧩 Компонент AddCategory

Компонент **AddCategory** уже создан в проекте и служит основой для будущего расширения.  
В дальнейшем он будет отвечать за добавление своих собственных категорий задач (например: Work, Home, Education).

> ⚠️ _Сейчас функционал категорий не реализован полностью_, так как для него требуется единая backend-база данных. MockAPI использует разные коллекции, что не позволяет синхронизировать данные полноценно.

---

### 🔧 Реализовано полностью:

- **Task Status** — добавление, редактирование, удаление
- **Task Priority** — добавление, редактирование, удаление

> ⚠️ **Примечание:**  
> Из-за ограничений MockAPI (разные базы для задач и статусов/приоритетов)  
> синхронизация может работать неполноценно.  
> В частности, обновление статуса не всегда корректно применяется ко всем задачам,  
> потому что статусы и сами задачи хранятся в отдельных MockAPI-эндпоинтах.
>
> После перехода на единый backend функциональность будет работать полностью корректно.

### 🚧 В разработке:

- **Task Categories** — будут добавлены после переноса проекта на реальный backend

### ✔ Настройки пользователя (Settings / Change Password)

- Страница **Settings**:
  - Настройки профиля пользователя
  - Настройки приложения (UI/UX-настройки в рамках курсового проекта)
- Страница/компонент **ChangePassword**:
  - Форма смены пароля
  - Подготовка к интеграции с backend (валидация и обработка ошибок могут быть расширены)

### ✔ Страница помощи (Help)

Страница **Help** добавлена в проект как базовый шаблон и демонстрация структуры приложения.  
На текущем этапе содержит минимальный контент:

- Заголовок страницы
- Краткий описательный текст

Используется как:

- 📌 Демонстрация дополнительной навигации в Sidebar
- 📌 Задел для будущего расширения (FAQ, гайды, подсказки по использованию приложения)

> ⚠️ **Примечание:**  
> Полный функционал Help будет реализован после подключения backend и добавления реального справочного контента.  
> Сейчас страница выступает как placeholder, чтобы показать структуру приложения и маршрутизацию.

### ✔ Header и календарь

- **Header**:
  - Поле поиска
  - Индикация текущей даты
  - Пометка **· Today** для сегодняшнего дня
  - Выпадающий календарь для выбора даты
- Выбор даты синхронизируется с задачами и влияет на отображение списка.

### ✔ DashboardHeader

- Компонент **DashboardHeader**:
  - Аватары пользователей
  - Кнопка **Invite**
  - Статус и краткое резюме по задачам
- Дополняет основную страницу Dashboard и улучшает UX.

### ✔ Redux Toolkit

- Используются:
  - `tasksSlice`
  - `paginationSlice`
  - `userSlice`
- `thunks`:
  - `fetchTasks`
  - `addNewTask`
  - `removeTask`
  - `updateTaskStatus`
- `selectors` для выборки задач, состояния загрузки, выбранной даты и т.д.
- Структура, готовая для подключения реального backend API.

---

## 🛠 Используемые технологии

### Frontend

- React 18
- TypeScript
- Vite
- Redux Toolkit
- React Router
- React Icons

### 📡 API

Проект использует модульную архитектуру API.  
Каждый функциональный блок вынесен в отдельный файл:

- `apiTasks.ts` — работа с задачами (CRUD, поиск, статусы)
- `apiUsers.ts` — пользователи, профиль, авторизация
- `apiMeta.ts` — meta-данные (приоритеты, статусы, системные значения)
- `statusApi.ts` — управление кастомными статусами задач
- `priorityApi.ts` — управление кастомными приоритетами

Архитектура полностью готова к подключению backend  
(например: Settings, ChangePassword, Categories и другие модули).

---

# 🎨 Соответствие макету (Figma)

Проект разработан на основе дизайн-макета:

**Figma:**  
https://www.figma.com/design/dpIQhX3dQBVPke5Yg4Qnor/To-do-List-Web-App-Design--Community-?m=auto&is-community-duplicate=1&fuid=942496748580674138

Интерфейс, структура страниц, визуальный стиль и логика работы максимально соответствуют макету.  
Некоторые элементы были улучшены для повышения удобства пользователя и корректной логики приложения.

---

## ✔ Что реализовано по макету

### Страницы и структура

- Dashboard
- My Tasks
- Vital Tasks
- Completed Tasks
- Sidebar
- Header
- Task Details блок

### Компоненты

- `TaskCard`
- `TaskDetails`
- `TaskStatus`
- `TaskPage`
- Модальные окна:
  - `AddTaskModal`
  - `EditTaskModal`
  - `TaskDetailsModal`

### Визуальная часть

- Цветовая схема
- Отступы
- Сетка и композиция
- Тени
- Базовая адаптивная верстка

---

## ✔ Улучшения сверх макета

### Дополнительные страницы

- **Categories** — управление категориями, статусами и приоритетами задач.
- **Settings** — настройки пользователя и приложения.
- **ChangePassword** — смена пароля (UI, подготовленный к интеграции с backend).
- **Help** — справочная страница.

### Completed Tasks

- Полностью реализованная функциональная страница
- Рабочая пагинация
- Корректное определение активной карточки при переключении страниц

### Пагинация

- Независимая пагинация для:
  - My Tasks
  - Vital Tasks
  - Completed Tasks
- Сохранение выбранной страницы при переходах

### Работа с датами

- Отображение текущей даты
- Пометка **· Today**
- Fallback-дата, если задач на выбранный день нет (отображается предыдущая доступная дата)
- Выбор даты через выпадающий календарь в Header

### Поиск

- Поиск по `title` и `description`
- Работает по всем датам и категориям
- Интегрирован в Header

### Task Details улучшения

- Исправлена логика выбора активной задачи
- Корректная работа при смене страницы/пагинации
- Мобильная адаптация блока Task Details

### UX-улучшения

- Предсказуемая навигация
- Удобная работа с задачами на всех страницах
- Реалистичный сценарий использования (в т.ч. с Completed / Vital / Categories / Settings)

---

## ✔ Верстка

- Верстка не является полностью pixel-perfect
- Структура и стиль максимально приближены к макету
- Некоторые элементы сознательно адаптированы для лучшего UX и логики приложения

---

## 🧩 Дополнительно

### Отдельная ветка с версткой

В GitHub-репозитории доступна отдельная ветка с исходной версткой проекта:

- **Ветка:** `html-css`
- Содержит базовую HTML/CSS-версию на React
- Использовалась как стартовая основа для дальнейшей разработки приложения

---

## 🏗 Архитектура

Проект организован по подходу **Feature-Sliced Design (FSD)**.

```bash
src/
├── main.tsx                     # Точка входа
├── index.css                    # Глобальные стили
│
├── app/                         # Инфраструктура приложения
│   ├── providers/               # Провайдеры: router, redux, auth
│   │   └── RouterProvider.tsx   # Провайдер маршрутизации (например)
│   │
│   ├── store/                   # Хранилище Redux Toolkit
│   │   ├── store.ts             # Конфигурация store
│   │   └── index.ts             # Экспорт useAppDispatch/useAppSelector
│   │
│   ├── routes/                  # Маршрутизация приложения
│   │   └── AppRouter.tsx        # Основной файл роутинга
│   │
│   ├── styles/                  # Глобальные стили
│   │   ├── App.css
│   │   └── variables.css (если появится)
│
├── pages/                       # Страницы (Routes)
│   ├── auth/
│   │   └── ui/
│   │       ├── LoginPage.tsx
│   │       └── RegisterPage.tsx
│   │
│   ├── dashboard/
│   │   └── ui/Dashboard.tsx
│   │
│   ├── my-task/
│   │   └── ui/MyTask.tsx
│   │
│   ├── vital-task/
│   │   └── ui/VitalTask.tsx
│   │
│   ├── completed-task/
│   │   └── ui/CompletedTask.tsx
│   │
│   ├── categories/
│   │   └── ui/CategoriesPage.tsx
│   │
│   ├── settings/
│   │   └── ui/
│   │       ├── SettingsPage.tsx
│   │       └── ChangePasswordPage.tsx
│   │
│   └── help/
│       └── ui/HelpPage.tsx
│
├── widgets/                     # Крупные UI-части (композиции)
│   ├── Header/
│   ├── Sidebar/
│   ├── DashboardHeader/
│   ├── TaskStatus/
│   ├── TodoList/
│   ├── CompletedTask/
│   └── TaskPage/
│       ├── ui/TaskPage.tsx
│       ├── TaskPage.css
│       └── hooks/
│           ├── useFilteredTasks.ts
│           ├── useFallbackTasks.ts
│           ├── usePaginationTasks.ts
│           └── useTaskSelection.ts
│
├── features/                    # Узкие функциональные фичи
│   ├── invite/                  # 🔥 Функция "Invite": модалка + логика + API
│   │   ├── api/
│   │   │   └── inviteApi.ts     # API инвайтов
│   │   ├── model/
│   │   │   └── useInvite.ts     # Хук + бизнес-логика
│   │   └── ui/
│   │       ├── InviteModal.tsx  # UI модалки
│   │       ├── InviteModal.css
│   │       └── index.ts         # Экспорт фичи
│   │
│   └── (можно добавлять дальше: search, filters, upload, notifications...)
│
├── entities/                    # Доменные сущности (DDD)
│   ├── task/
│   │   ├── api/
│   │   │   └── todos.ts
│   │   ├── model/
│   │   │   ├── tasksSlice.ts
│   │   │   ├── paginationSlice.ts
│   │   │   └── index.ts
│   │   ├── TaskCard/
│   │   │   ├── ui/TaskCard.tsx
│   │   │   ├── TaskCard.css
│   │   │   └── index.ts
│   │   └── ui/
│   │       ├── AddTaskModal/
│   │       ├── EditTaskModal/
│   │       ├── TaskDetails/
│   │       └── TaskDetailsModal/
│   │
│   ├── priority/
│   │   └── api/
│   │       └── priorityApi.ts
│   │
│   ├── status/
│   │   └── api/
│   │       └── statusApi.ts
│   │
│   └── user/
│       ├── api/
│       │   └── apiUser.ts
│       └── model/
│           ├── userSlice.ts
│           └── memberTypes.ts
│
├── shared/                      # Переиспользуемые модули
│   ├── api/
│   │   └── api.ts               # axios instance, setAuthToken
│   ├── assets/
│   │   └── images
│   └── ui/
│       └── (кнопки, инпуты, лоадеры, иконки и пр.)
│
└── data/                        # Данные / mocks / JSON
```
