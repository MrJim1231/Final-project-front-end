# 🧩 Backend — FSD-FULL

Полноценный backend, реализованный на **Node.js + Express**,  
построенный по архитектуре **MVC + Services**, с модульным разделением, системой авторизации, задачами, приглашениями и инициализацией базы данных.

---

## 📁 Структура проекта

```bash
backend/
│
├── controllers/        # Логика обработки запросов (вход/выход данных)
├── middleware/         # JWT-проверка, обработка ошибок, логирование
├── models/             # Mongoose-схемы (User, Task, Priority, Status и др.)
├── node_modules/       # Установленные зависимости
├── routes/             # Роуты, привязка URL к контроллерам
├── services/           # Бизнес-логика, работа с БД, приглашения и т.д.
├── utils/              # Хелперы: токены, email, валидации, генераторы
│
├── .env                # Конфигурация окружения (JWT, MongoDB, SMTP и пр.)
├── initDefaults.js     # Инициализация дефолтных статусов / приоритетов / ролей
├── server.js           # Точка входа в приложение, запуск Express-сервера
└── testDb.js           # Тестовое подключение / инициализация базы данных
```

---

## 🚀 Что реализовано

### 🔐 **Авторизация и аутентификация**

- Регистрация пользователя
- Логин по email/username и паролю
- Генерация и проверка **JWT-токенов**
- Middleware для защиты приватных маршрутов
- Хранение токена на клиенте (поддержка фронтенда)

---

### 📝 **Работа с задачами (Tasks)**

- Создание, редактирование, удаление задач
- Привязка задачи к:
  - `status`
  - `priority`
  - `ownerId`
- Поддержка изображений в задачах
- Поля `createdAt`, `completedAt`, `vital`
- Гибкое расширение модели

---

### 🟦 **Статусы и приоритеты**

- Модели Status и Priority
- CRUD-операции
- Seed-инициализация через `initDefaults.js`
- Автоматическое создание базовых значений

---

### ✉️ **Система приглашений (Invites)**

- Генерация приглашений с определённой ролью
- Отправка письма с ссылкой на регистрацию
- Регистрация через invite-токен
- Создание `Member` при успешной активации
- Удаление использованных приглашений

---

### 👥 **Роли пользователей**

- owner / view / edit / admin
- Привязка роли к Member
- Возможность ограничивать роуты через middleware

---

### ⚙️ **Техническая архитектура**

- Чистая структура Controller → Service → Model
- Логическое разбиение кода по модулям
- Повторное использование сервисов
- Простое масштабирование (легко добавлять новые модули)

---

## 🛠 Используемые технологии

| Технология             | Назначение             |
| ---------------------- | ---------------------- |
| **Node.js**            | Серверное окружение    |
| **Express.js**         | Backend-фреймворк      |
| **MongoDB + Mongoose** | База данных и ORM      |
| **JWT**                | Авторизация            |
| **bcryptjs**           | Хеширование паролей    |
| **dotenv**             | Конфигурация окружения |
| **Nodemailer / SMTP**  | Email-отправка         |

---

## 📦 Установка и запуск

### 1️⃣ Установить зависимости

```bash
cd backend
npm install
```
